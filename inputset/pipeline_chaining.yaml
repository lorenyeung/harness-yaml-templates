inputSet:
  name: pipeline_chaining_<+pipeline.executionId>
  tags: {}
  identifier: pipeline_chaining_<+pipeline.executionId>
  orgIdentifier: <+org.identifier>
  projectIdentifier: <+project.identifier>
  pipeline:
    identifier: pipeline_chaining
    stages:
      - stage:
          identifier: echo
          type: CI
          spec:
            execution:
              steps:
                - step:
                    identifier: echo
                    type: Run
                    spec:
                      envVariables:
                        input: "hello"
            infrastructure:
              type: KubernetesDirect
              spec:
                connectorRef: "blah"

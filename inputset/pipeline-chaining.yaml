inputSet:
  name: <+pipeline.identifier>_<+pipeline.executionId>
  tags: {}
  identifier: <+pipeline.identifier>_<+pipeline.executionId>
  orgIdentifier: <+org.identifier>
  projectIdentifier: <+project.identifier>
  pipeline:
    identifier: <+pipeline.identifier>
    stages:
      - stage:
          identifier: echo
          type: CI
          spec:
            execution:
              steps:
                - step:
                    identifier: echo
                    type: Run
                    spec:
                      envVariables:
                        input: "hello"
            infrastructure:
              type: KubernetesDirect
              spec:
                connectorRef: "blah"
